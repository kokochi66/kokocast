import React from 'react';

const VideoStreamArea: React.FC = () => {
    return (
        // 동영상을 표시할 영역이나, 일단 임시 레이아웃만 잡으면 되기 때문에, 까만 화면을 표시합니다.
        // 나중에 실제 동영상을 표시할 수 있도록 수정해야 합니다.
        // 구현
        <div id="video-stream-area">
            VideoStreamArea
        </div>
    );
};

export default VideoStreamArea;
